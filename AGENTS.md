# プロジェクト規約: 経済ニュースダイジェスト

## 目的

個人投資家向けの経済ニュースダイジェストを、JST 06:00 / 12:00 / 22:00 に発行する。公開先は GitHub Pages の `docs/` である。

## 技術スタック

- ビルドツールなしの静的サイト（HTML + CSS）
- GitHub Pages の公開ルートは `docs/`
- Web 定期実行は GitHub プラグイン経由でリポジトリを更新する

## ファイルの役割

- `docs/index.html`: 最新号
- `docs/archive/<号ID>.html`: 過去号
- `docs/assets/style.css`: 共通スタイル
- `data/seen.json`: 直近72時間の重複排除台帳
- `data/history.json`: 発行履歴
- `AUTOMATION.md`: Web 定期実行の完全な手順
- `SCHEDULE_PROMPT.md`: ChatGPT Web の Scheduled task に登録するプロンプト

## 発行規約

- 号IDはスケジュール枠に対応する `YYYY-MM-DD-HHmm`。分は必ず `00` とする。
- 1号につき5〜7件を基本とする。重要ニュースが少ないときは2〜3件でよい。
- 日本、米国、世界のニュースを可能な範囲でバランスさせる。
- `docs/index.html` を更新する前に、旧indexをアーカイブ向け相対パスへ変換して保存する。
- `seen.json` と `history.json` を毎号更新する。
- 同じ号IDを二度発行しない。

## 情報品質

- 検索結果のスニペットだけで記事を書かず、採用するURLを実際に開いて確認する。
- 公開日時が発行時刻から72時間以内の記事だけを採用する。
- URL、記事名、数値、発言、公開日時を推測で作らない。
- 相場数値には対象日時を明記する。
- 重要な数値や政策判断は、一次情報または独立した複数の信頼できる出典で照合する。
- 原文を転載せず独自に要約し、各トピックに出典リンクを付ける。
- 末尾に投資助言ではない旨の免責を必ず記載する。

## GitHub更新規約

- 定期実行は `main` の現在の先端を親にした単一コミットで全ファイルを更新する。
- コミットメッセージは `digest: <号ID>` とする。
- PRや別ブランチを作らず、fast-forwardの場合だけ `main` を更新する。
- 親コミット取得後に `main` が進んでいた場合は更新せず、最新状態からやり直す。
- force pushを禁止する。
